# Cycle Tracker: PWA example

This is a simple Progressive Web App meant to serve as an example showing how to create a PWA.

To run the app:

1. Clone the repo.
2. Open a command line prompt in the directory you cloned the repo in.
3. Type `npx http-server` to start a local web server.
4. Using your browser, go to `http://localhost:8080`.
