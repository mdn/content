name: "PR conflicts"

on:
  push:
  pull_request_target:
    types: [synchronize]

concurrency:
  group: pr-conflicts-${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  labeler:
    uses: mdn/workflows/.github/workflows/pr-rebase-needed.yml@main
    with:
      target-repo: "mdn/content"
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
